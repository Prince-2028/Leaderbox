// ClaimButton.js
// This component will be a button to claim something (e.g., a reward).

import React from 'react';

const ClaimButton = () => {
  return (
    <button>
      Claim
    </button>
  );
};

export default ClaimButton;
